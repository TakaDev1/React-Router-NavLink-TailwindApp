# 課題

### 問題文

現在表示しているページのナビゲーションを青色＋太字にし、その他のナビゲーションをグレーにしてください。

### 条件

* `/` → `Home`
* `/profile` → `Profile`
* `/settings` → `Settings`
* アクティブ状態

  * `text-blue-500`
  * `font-bold`
* 非アクティブ状態

  * `text-gray-500`
* `NavLink`の`isActive`を使用する